# 补充核查报告与创新点实施方案

> 本文档是对《代码对照与修改计划.md》的独立复核与补充，包含：①原报告条目复核结论与实测证据；②新发现的问题；③题目原文逐条核对清单；④创新点代码级实现方案与优先级。
> 复核方式：通读全部代码 + 提取题目 docx 原文逐条对照 + 用实际数据对关键缺陷做数值测算。

---

## 一、复核总结论

1. **原报告总体成立**：A1–A5、B1–B7、C1–C5 全部复核属实（逐条见第二节）。公式、数据口径、模板列名、结果自洽性核对全部通过。
2. **A1 严重性大幅上调**（原报告列为"必须修复"，但低估了影响）。实测当前 4 个中继架次**全部**悬停于巡航海拔之上，按正确口径重算：
   - 往返时间被低估 **+93 ~ +125 s/架次**，合计 **+451 s**；能耗低估 **+0.16 kWh**；
   - 修正口径下 4 个任务能量仍满足返航余量（2.56 kWh 上限，修正后最大 2.30 kWh ✓），但**建链时限全部不满足**：每个任务需提前 47~63 s 出发，现有排程无法做到 → **当前 Q3 排程在正确物理模型下不可行，必须重跑 problem3**，且重跑后悬停点、时刻、甚至运输延迟均可能变化。
3. **新发现 5 个问题**（N1–N5，见第三节），其中 N1（目录布局）导致本仓库内全流程不可直接复跑，N2（硬时限保护失效）是潜在的约束破坏漏洞。

---

## 二、原报告条目复核明细

### A 类（必须修复）——全部属实，A1 升级

| # | 复核结论 | 实测证据 |
| :--- | :--- | :--- |
| A1 | **属实，严重性升级**。[common.py:388-389](code-wb/code/common.py#L388-L389) `relay_fly` 用 `max(0,·)` 截断爬升/下降高度 | 4 个中继悬停点（z=707/670/747/591 m）全部高于各自航线巡航海拔（520/440/511/341 m）。修正口径（爬升至 `max(h_cruise, h_hover)`）后：时间 +451 s、能耗 +0.16 kWh；**4 个任务建链时限全部不满足（需提前 47~63 s）**。重跑后 Q3 方案（悬停点/时刻/延迟量/联合 Cmax=12206s）可能整体变化 |
| A2 | **属实**，[problem3.py:135-147](code-wb/code/problem3.py#L135-L147) 越界候选点被 `dem_elev` 静默钳制 | 实测当前缺口包围盒扩展后 x∈[-7845,6898]、y∈[-1867,9395]，DEM 覆盖 x∈[-20273,21946]、y∈[-16359,24042] → **当前 0 个越界点，仅潜在缺陷**，但必须修（题目明确要求"悬停位置应位于 DEM 覆盖范围内"） |
| A3 | **属实**，[problem3.py:583-587](code-wb/code/problem3.py#L583-L587) 未覆盖区间仍写"中继"且编号为空 | 当前运行 0 未覆盖、Q3_通信保障.csv 无空编号行（实测 16 行中继行全部有编号），防御性修复仍需做 |
| A4 | **属实**，[_verify_q3_link.py:5,8](code-wb/code/_verify_q3_link.py#L5) 硬编码 `D:\desktop\D题\code` | 该路径在本仓库根本不存在，脚本无法运行。且见 N1：**不止这个脚本**，全部脚本都依赖特定目录布局 |
| A5 | **属实**，[problem3.py:159-175](code-wb/code/problem3.py#L159-L175) 抽检 ≤15 点可能漏检遮挡 | 另见 N3：最终验证脚本本身也不是"全分辨率"（同 5 s 采样、只查接入链路），A5 的修复目标需一并落到验证脚本上 |

### B 类（建议修复）——全部属实

| # | 复核结论 |
| :--- | :--- |
| B1 | **属实**。[problem2.py:248-277](code-wb/code/problem2.py#L248-L277) `repair` docstring 称"regret 插入"实为最便宜插入；[problem2.py:250-251](code-wb/code/problem2.py#L250-L251) `random.shuffle(removed)` 后立即 `sort` 确为死代码 |
| B2 | **属实**。[problem4.py:6-8](code-wb/code/problem4.py#L6-L8) docstring 声称中继合并任务块，但 [task_blocks](code-wb/code/problem4.py#L49-L58) 只合并运输架次——**行为正确**（题目原文只要求运输架次涉及的服务区同组），跨组中继确在 [evaluate_partition:116-118](code-wb/code/problem4.py#L116-L118) 按保守口径双计 |
| B3 | **属实**。[common.py:431-433](code-wb/code/common.py#L431-L433) `build_and_solve` 死代码 |
| B4 | **属实**。[problem1.py:91](code-wb/code/problem1.py#L91) 未用 `w`、[export_results.py:13](code-wb/code/export_results.py#L13) 未用 `TEMPLATE`、[problem3.py:34](code-wb/code/problem3.py#L34) 未用 `GAP_MAX` |
| B5 | **属实**。[problem1.py:18](code-wb/code/problem1.py#L18) `get_copt_model` 导入未使用 |
| B6 | **属实**。[problem3.py:487](code-wb/code/problem3.py#L487) 循环内重定义 `BOX` |
| B7 | **属实**。说明.md 出现两个 `## 6`（第 168 行与第 183 行 `## 6.1` 应为 `## 7`） |

### C 类（口径声明）——与题目原文核对后确认全部必要

题目原文核对结果：等效航程、航段时间、两段充电、FSPL、双向链路、遮挡附加损耗、服务三态判定**均与代码逐式一致**；而 `E_hor`、`E_up` 题目确实**未给显式公式**（附录 2 只给能耗构成式 `E = E_hor + E_up`），C1 的披露口径是必需且最自然的选择。C2–C5 均有题目原文依据（详见第四节）。

---

## 三、新发现的问题（N1–N5）

### N1【高】目录布局与代码内路径推导不一致，本仓库内全流程不可复跑

[common.py:21](code-wb/code/common.py#L21) `ROOT = dirname(dirname(abspath(__file__)))` 假设代码位于 `D题/code/`，数据在 `D题/数据/`。本仓库代码在 `code-wb/code/`，数据在 `D题/数据/` → 实测 `ProblemData()` 直接 `FileNotFoundError`。[export_results.py:13](code-wb/code/export_results.py#L13) 的模板路径同理。

**修复（二选一，推荐两者都做）**：
1. 提交/复跑时将 `code-wb/code/` 复制为 `D题/code/`（与说明.md 的目录树一致）；
2. 让 `ROOT` 解析更鲁棒：从 `__file__` 向上逐级查找同时含 `数据/` 与 `结果提交模板.xlsx` 的目录，找不到再报错。

### N2【高】联合调整的硬时限保护失效（静默违反医疗/首批硬时限）

[problem3.py:515](code-wb/code/problem3.py#L515) `if best is None or best[1] > 1e9` 判断的是**原始延迟量 d**，而非含 `hard_penalty` 的 key（[problem3.py:489-499](code-wb/code/problem3.py#L489-L499) 每个违约箱惩罚 1e6）。当所有可排候选都必然违反硬时限时，只要 d < 1e9，代码仍会应用该延迟，**静默破坏 Q2 已保证的 0 超时**。

**修复**：改为 `if best is None or best[0] >= 1e6: 报错终止并提示"需增加中继资源或调整运输方案"，不应用违反硬时限的延迟**。

### N3【中】_verify_q3_link.py 并非"全分辨率"复核

该脚本用与 problem3 相同的 DT=5 s 重建轨迹（采样分辨率没有提高），且只复核接入链路、不复核回传链路；"服务窗口包含该区间"是必要条件但未检查区间级唯一提供方（好在 [plan_relays:329-336](code-wb/code/problem3.py#L329-L336) 的 `new_cover` 去重逻辑已保证区间级互斥，此点代码本身正确）。

**修复**：验证脚本以 DT=1 s 重建轨迹，接入+回传同时复核，并输出每条链路的最小裕量（同时实现创新点 7）。

### N4【低】task_blocks 的 relays 参数未使用

[problem4.py:49](code-wb/code/problem4.py#L49) `task_blocks(routes, relays)` 中 `relays` 未使用（B2 修正 docstring 时一并处理：删除参数或注明保留接口）。

### N5【低】q3_solution.json 丢失区间级覆盖信息

[problem3.py:594-601](code-wb/code/problem3.py#L594-L601) 存盘时 `cover` 只保留架次编号，区间→中继的完整映射只在 Q3_通信保障.csv 中。验证脚本只能按"窗口包含"近似核对，无法精确复核"每个缺口区间恰好由一个中继全程覆盖"。

**修复**：`cover` 同时存区间索引 `itv_idx`（或 t_s/t_e 列表）。

---

## 四、题目原文核对通过清单（供论文引用）

### 4.1 公式级核对（与题目 docx 原文逐式对照，全部一致）

| 公式/规则 | 题目 | 代码位置 |
| :--- | :--- | :--- |
| 等效航程 `L(q)=L0-(L0-LF)(q/Q)^{3/2}` | 附录2 ✓ | [common.py:266-270](code-wb/code/common.py#L266-L270)（`ratio**1.5`） |
| 航段时间 `t=h+/v↑+d/vc+h-/v↓` | 附录2 ✓ | [common.py:275](code-wb/code/common.py#L275) |
| 能耗构成 `E=E_hor+E_up`、下降效率 0 | 附录2 ✓ | [common.py:277-280](code-wb/code/common.py#L277-L280) |
| 返航余量 `ΣE≤(1-ρ)Euse` | 附录2 ✓ | [common.py:309](code-wb/code/common.py#L309) |
| 巡航海拔=航线最高 DEM+50m；O01 取地面海拔、服务区+30m | 附录2 ✓ | [common.py:256-261](code-wb/code/common.py#L256-L261)、[common.py:187-190](code-wb/code/common.py#L187-L190) |
| 两阶段充电 65%/35% 线性折算 | 附录2 ✓（手算 s=0 时恰为 Tfull） | [common.py:328-333](code-wb/code/common.py#L328-L333) |
| `Pth=Psens+M=-98+8=-90 dBm` | 附录3 ✓ | [common.py:341-343](code-wb/code/common.py#L341-L343) |
| `Lmax=Ptx+Gtx+Grx-Lsys-Pth` 双向取小 | 附录3 ✓ | [common.py:345-364](code-wb/code/common.py#L345-L364)，实测 122/116/126 dB |
| `FSPL=32.45+20lg f+20lg D(km)` | 附录3 ✓ | [common.py:370](code-wb/code/common.py#L370) |
| `Lpath=FSPL+Lobs·b`、遮挡 +10 dB | 附录3 ✓ | [common.py:366-376](code-wb/code/common.py#L366-L376) |
| 服务状态：直连→中继→中断三态 | 附录3 ✓ | [problem3.py:100-129](code-wb/code/problem3.py#L100-L129) |
| 中继能耗：巡航功率×时间、爬升同关系（计划起飞总质量）、悬停+通信功率 | 附录2 ✓ | [common.py:379-394](code-wb/code/common.py#L379-L394)、[problem3.py:189-190](code-wb/code/problem3.py#L189-L190) |

### 4.2 规则级核对（口径合法性依据）

- **一架中继同时服务多架运输无人机**：题目只写"每架运输无人机在任一时刻只能由 G01 或一架中继无人机提供通信保障"（附录3），未限制中继的并发服务数 → 代码口径合法，且 [plan_relays:329-336](code-wb/code/problem3.py#L329-L336) 已保证区间级唯一提供方 ✓
- **Q4 只要求运输架次涉及的服务区同组**：题目原文"若同一运输架次同时涉及多个服务区，则这些服务区应划入同一任务组"，未提及中继 → [task_blocks](code-wb/code/problem4.py#L49-L58) 行为正确，跨组中继保守双计需在论文说明 ✓
- **Cmax 口径**：Q2"所有运输无人机完成最后一个架次并返回 O01 的最晚时刻"、Q3"所有运输无人机和中继无人机……的最晚时刻" → [problem3.py:532-533](code-wb/code/problem3.py#L532-L533) 取 max(运输 end, 中继 ret_act) ✓
- **悬停位置在 DEM 内、离地 ≤ h_max**：题目明确要求，h_max=300 m 与 HOVER_HEIGHTS=[60..300] 吻合 ✓（A2 修复即为此条）
- 初始 SOC=100%、同机型电池不可混用、充电占用不重叠、架次间须充至 100%：代码 [problem2.py:109-128](code-wb/code/problem2.py#L109-L128)、[problem3.py:311-327](code-wb/code/problem3.py#L311-L327) ✓
- 数据侧实测：中继 m=23.5 kg、Pc=1.15 kW、Euse=3.2 kWh、ρ=20%、η_up=0.72、P_hover=1.05 kW、P_comm=0.05 kW ✓；结果提交模板 6 个 sheet 列名与 export 映射逐列一致 ✓

### 4.3 结果自洽性复核（抽样）

- q2_solution.json：17 架次、hard=0、Cmax=12009 s、E=65.577 kWh ✓ 与说明.md 一致
- q3_solution.json：16 个缺口区间、4 个中继架次全覆盖、联合 Cmax=12206 s ✓
- Q3_通信保障.csv：16 行"中继"行全部有编号（当前 0 未覆盖）✓
- q4_report.json：K=2/3 四种方案（资源最小/均衡最优 ×2）✓

---

## 五、创新点实现方案（代码级设计）

> 优先级：P0=正确性（先修再谈创新）；P1=低成本高收益，建议全部做；P2=论文亮点，时间允许必做；P3=可选对比方案。

### P0 — 正确性修复（A1/A2/A3/A4/A5 + N1/N2/N3）

按原报告第五节实施，重点补充：
1. **A1 修复后必须重跑 problem3**，并复核：建链时限全部满足、无运输延迟或延迟不违反硬时限（N2 修复后自动保证）、联合 Cmax 更新；
2. 验证脚本升级（N3）：DT=1 s 全分辨率 + 接入/回传双链路 + 裕量输出；
3. 若修复后中继架次数或悬停点变化，**同步重跑 problem4 并更新结果与图表**（q4 依赖 q3 的 relays 时间轴）。

### P1 — 低成本高收益创新（建议全部实现，半天内可完成）

**I-1 Q2 Pareto 权衡分析**（题目硬性要求"说明各指标之间的权衡关系"）
- 新建 `code-wb/code/analysis_q2_pareto.py`：对 (W_TARD, W_CMAX, W_SORT) 取 9~12 组代表性权重（如 W_TARD∈{0.1,1,10}×W_CMAX∈{1,5,20}），每组跑 `alns(seed=2026, iters≈800)`；
- 收集 (加权tardiness, Cmax, 能耗, 架次数) 四元组，做非支配过滤，绘制 2D 投影散点 + 平行坐标图（复用 visualize.py 风格）；
- 产出：Pareto 前沿图 + 权衡说明表（论文直接引用）。

**I-2 Q2 下界认证**（证明 17 架次/12009 s 解的质量）
- 新建 `code-wb/code/analysis_q2_bounds.py`，构造三个下界：
  1. **质量下界**：`Σ_area ⌈Σw_area/Q_C⌉`（只用最大机型 C，忽略体积/能量）；
  2. **能量下界**：每箱按"能耗最小的单点往返"计，`Σ_box min_g E(g,area(b),w_b)/Euse_C`；
  3. **时间下界**：`max(Σ_area 最短单点往返时间×架次下界 / 8 架无人机, 各机型的质量下界×最快单点往返)`；
- 产出：下界对比表（gap%）——若 17 架次接近质量下界，即为"近优"论据。

**I-7 通信余量报告**（鲁棒性论据，几乎零成本）
- 在 problem3 输出 `Q3_通信余量.csv`：每个缺口区间输出 `min_t(Lmax_UR-Lpath_UR(t))` 与 `min_t(Lmax_RG-Lpath_RG(t))`；
- 追加敏感性：DEM ±5 m 扰动后重跑缺口识别，统计中继方案仍 100% 覆盖的比例。

**I-8 Q4 分区数扫描**
- [problem4.py:187](code-wb/code/problem4.py#L187) 的 `for K in (2,3)` 改为 `(1,2,3,4)`（7 个任务块、K=4 时 4^7=16384 枚举仍秒级）；
- 产出："分区数 K—总资源/资源缺口"曲线，揭示重复配置的规模效应，增强结论说服力。

**I-9 Q4 工作量指标敏感性**
- [group_resources](code-wb/code/problem4.py#L77-L102) 的 workload 参数化为三种口径：作业时间 / 架次数 / 货箱质量，对比均衡方案差异。

### P2 — 论文亮点创新（时间允许必做）

**I-3 Q2 真 regret-k 插入 + 跨架次 swap 算子**（顺带解决 B1 的文档与实现不符）
- `repair` 改为 regret-2：对每箱求所有候选插入的 ΔE，取"次优与最优差最大"的箱优先插入（10 行改动）；
- 新增 destroy 算子：随机选两架次各取一箱交换（`route_feasible` 校验，[problem2.py:280-315](code-wb/code/problem2.py#L280-L315) 处新增 `destroy_swap`）；
- 可选增强：插入时按"插入后该机型电池最晚空闲时刻最早"引导（排程感知插入）。

**I-4 Q3 通信感知的联合优化（候选解选择式）**——把"延迟修正"升级为"联合优化"
- 重构：把 problem3 的核心封装为 `evaluate_q3(routes)->(relay_plan, n_relay, E_relay, n_delay)`（现有 main 的 1~4 步）；
- problem2.alns 增加 `top_k` 收集（按 obj 排序保留前 K≈5 个近优运输解）；
- 新入口 `problem3.run_joint(K=5)`：对每个近优解跑 `evaluate_q3`，按联合目标 `obj_Q2 + λ·(n_relay, E_relay)` 选取最优组合；
- 产出：联合最优方案 + "运输最优 vs 联合最优"对比表（说明通信成本如何反向影响运输规划——这正是题目 Q3 权衡分析的核心）。

**I-5 悬停位置连续精修**
- 对 MILP 选出的每个悬停点，用 `scipy.optimize.minimize`（Nelder-Mead/SLSQP）在 (x,y,z) 上最大化覆盖余量 `min_t min(Lmax_UR-Lpath_UR, Lmax_RG-Lpath_RG)`，约束：DEM 界内、z-terrain(x,y)≤h_max、链路可行（惩罚项）；
- 精修后重算 `relay_fly` 能量/时间验算并回写方案；
- 产出：精修前后余量对比（方案对地形误差的鲁棒性论证）。

**I-10 Q3 权衡曲线（中继架次 ↔ 允许运输延迟）**
- 对联合迭代中延迟的硬惩罚系数 λ 扫描（如 λ∈{0,1e3,1e6,∞}），输出"中继架次数—累计运输延迟"权衡曲线，直接回应 Q3"说明各指标之间的权衡关系"。

### P3 — 可选对比方案

**I-6 多悬停点中继任务**
- 修改 [plan_relays](code-wb/code/problem3.py#L234-L349) 的候选任务生成：两点间距 < 1 km 且分别覆盖相邻缺口时，生成"两段服务+中间转移"任务（转移段按 `relay_fly` 相邻点计算爬升/巡航/下降）；
- 作为对比方案写入论文：中继架次数可压缩到 2~3 架次的代价是任务复杂度与转移能耗。

---

## 六、修复后重跑与验证清单

1. `python problem1.py`（确认无变化）
2. `python problem2.py`（确认 17 架次/12009 s 复现）
3. `python problem3.py`（A1/N2 修复后：建链时限全满足、0 未覆盖、0 硬违约延迟、联合 Cmax 更新）
4. `python problem4.py` → `python export_results.py` → `python visualize.py`
5. 运行升级版 `_verify_q3_link.py`（DT=1 s）：全部区间全分辨率覆盖、裕量 > 0
6. 检查 `results/结果提交.xlsx` 六 sheet 行数/列名与模板一致
7. 实施 P1 创新点，生成 Pareto 图、下界表、通信余量表、K 扫描曲线并更新说明.md/README
