# D题代码对照检查报告与修复计划

## Context

用户已完成华为杯D题四个问题的全部求解代码（code-wb/code/，含 common.py、problem1-4.py、export/visualize、说明.md 与结果文件），要求对照题目做三方面检查：①代码规范与语法问题；②对题目理解的正确性、数据来源合规性、解题思路可行性；③在现有基础上提出更创新的求解方案。本报告为审查结论 + 建议修复清单。

**已完成的审查动作**：通读全部10个代码文件与结果JSON；从官方Word文档提取OMML公式逐条核对；抽查结果数据（P2-001架次手算时间/能耗与代码输出完全一致：T=1551.69s、E=1.8199kWh ✓）；核查首批30箱硬时限（0超时 ✓）；核查电池占用-充电互斥链（BC01等全部无重叠 ✓）。

## 一、总体结论

**三个问题的回答：**

1. **代码规范**：整体规范良好（模块化清晰、注释充分、有缓存与向量化优化、含资源可行性自检）。无语法错误（__pycache__表明已实际运行过）。发现的问题均为小问题：1处硬编码路径、若干死代码/未使用变量、2处文档与实现不符、1处说明.md章节号重复。
2. **题目理解**：**正确**。已通过OMML公式提取确认：等效航程、航段时间、两段充电、FSPL/链路预算/遮挡判定等全部公式与题目原文一致。题目原文对 E_hor/E_up **未给显式公式**（只给和式与爬升能耗效率0.72），代码采用 `E_hor=E_use·d/L(q)`、`E_up=(m0+q)·g·h/η` 是自然口径且已在说明.md披露为假设 ✓。数据全部来自官方附件，数据继承链（Q1→Q2→Q3→Q4 JSON）清晰。求解思路可行：四问结果均已产出且内部自洽（首批0超时、资源互斥通过、能耗手算吻合）。
3. **创新方案**：见第四节，有明确的提升空间（Pareto权衡、下界认证、通信感知的联合优化等）。

## 二、问题清单（按优先级）

### A. 必须修复（正确性/稳健性）

| #    | 位置                                        | 问题                                                         | 修复                                                         |
| :--- | :------------------------------------------ | :----------------------------------------------------------- | :----------------------------------------------------------- |
| A1   | common.py:388-389 `relay_fly`               | 中继悬停海拔高于航线巡航海拔时（山顶+300m悬停），`h_up/h_dn` 用 `max(0,·)` 截断，**低估爬升时间与能耗**（该场景在回传通视候选点中实际存在） | 改为 `h_cruise_eff = max(h_cruise, h_hover)`，爬升以 `h_cruise_eff` 计，`h_dn = h_cruise_eff - h_hover` |
| A2   | problem3.py:135-147 `candidate_points`      | 缺口包围盒±1500m扩展后，越出DEM范围的候选点被 `dem_elev` 静默钳制为边界高程，**违反"悬停位置应位于DEM覆盖范围内"** | 候选点生成时用 `in_bounds` 过滤，越界点丢弃                  |
| A3   | problem3.py:583-587                         | 未覆盖区间的通信保障行仍写"中继"且中继架次编号为空 → 结果表失真（当前运行0未覆盖，但代码应防御） | 未覆盖区间写保障方式="中断"（或直接报错终止），并加断言      |
| A4   | `_verify_q3_link.py:5,8`                    | 硬编码 `D:\desktop\D题\code` 绝对路径，换机不可运行；该脚本是"通信全覆盖"的最终证据，必须可复现 | 改用 `os.path.dirname(__file__)` 相对定位，并入 problem3 主流程或独立可复现入口 |
| A5   | problem3.py:159-175 `point_covers_interval` | 覆盖判定只抽检≤15个时间点（max_check），长缺口区间可能漏检地形遮挡 | 区间≤75s全量检查；更长区间抽检后**必须**由最终验证脚本（A4修复后）全分辨率复核并输出余量 |

### B. 建议修复（文档一致性与代码整洁）

| #    | 位置                                                 | 问题                                                         | 修复                                            |
| :--- | :--------------------------------------------------- | :----------------------------------------------------------- | :---------------------------------------------- |
| B1   | 说明.md 第110行                                      | 声称"regret 插入修复"，实际 problem2.py:248-277 是**最便宜插入**（greedy），且 `random.shuffle` 后立即 sort 为死代码 | 文档改为"最便宜插入"或实现真 regret-2；删死代码 |
| B2   | problem4.py 模块docstring                            | 声称"同一中继架次服务多个架次→并入不可拆任务块"，但 `task_blocks` 只合并运输架次（**行为正确**，题目只要求运输架次合并；跨组中继已在 `evaluate_partition` 按保守口径双计） | 修正docstring，明确题目要求与保守核算口径       |
| B3   | common.py:421-433                                    | `milp_binary_solve` 中 `build_and_solve` 死代码              | 删除                                            |
| B4   | problem1.py:91、export_results.py:13、problem3.py:34 | 未使用变量 `w`、未使用 `TEMPLATE`、未使用 `GAP_MAX`          | 删除或注释说明                                  |
| B5   | problem1.py:18                                       | `get_copt_model` 导入未使用                                  | 删除                                            |
| B6   | problem3.py:487                                      | 循环内重定义 `BOX`（遮蔽模块级变量）                         | 删除重定义                                      |
| B7   | 说明.md 第6节                                        | 出现两个"## 6"（6与6.1）                                     | 重新编号                                        |

### C. 口径声明项（代码不动，论文中必须交代）

1. `E_hor = E_use·d/L(q)`、`E_up = (m0+q)·g·h/η_up` 为**参赛队假设口径**（题目未给显式公式）——说明.md已披露 ✓，论文正文需同样披露并给依据（等效航程定义自洽性）。
2. 服务区作业高度取**表格海拔**+30m（非DEM插值）——与"取其地面海拔"字面一致，论文说明。
3. 一架中继可同时服务多架运输无人机——题目只限制运输侧唯一提供方 ✓，论文给出依据。
4. Q2 的 `MAX_STOPS=3`、目标权重 `W_*` 为算法参数——论文给出敏感性。
5. Q4 跨组中继架次按保守口径在各组分别计入——论文说明。

## 三、审查通过的要点（无需改动，供论文引用）

- 全部公式与题目OMML原文逐条核对一致：L(q)（幂1.5）、航段时间、两段充电、Pth=-90dBm、Lmax双向取小、FSPL(km)、遮挡+10dB、服务三态判定
- 链路预算手算核对：122/116/126 dB ✓；C型最大安全载荷58.5kg(S008)等与模型一致
- 首批30箱全部在硬时限内（最紧的S001-MED-02仅64s余量）；电池/UAV链式排程无重叠；充电时间、SOC下限全部满足
- 结果模板6个sheet列名与官方模板逐一对应 ✓
- 求解框架四问递进、数据继承、可复现（固定种子）✓

## 四、创新方案建议（第3问的回答）

**Q2（增强现有框架，成本低收益高）：**

1. **Pareto权衡分析**：题目明确要求"说明各指标之间的权衡关系"——对 W_TARD/W_CMAX/W_ENER/W_SORT 做权重扫描，输出及时性-Cmax-能耗-架次数的Pareto前沿图（现有ALNS直接可复用，运行成本低）
2. **下界认证**：构造三个下界（质量下界 Σ⌈w/Q_C⌉、能量下界 ΣE_i/E_use_C、LP松弛集合覆盖下界），把17架次/12009s与下界对比，证明解质量
3. **路线-资源联合改进算子**：跨架次货箱交换（swap）算子、按"最晚电池空闲时刻"引导的插入；或对固定路线集用约束规划精确求解开始时刻（disjunctive scheduling，coptpy可解）

**Q3（差异化亮点）：** 4. **通信感知的运输规划**：保留Q2的top-K个近优解，对每个解做中继成本评估，选"运输+中继"联合最优——把现在"延迟修正式"迭代升级为"候选解选择式"联合优化 5. **悬停位置连续化精修**：MILP选出格点后，对悬停点(x,y,AGL)做局部连续优化（坐标下降/模式搜索），最大化覆盖余量（接入+回传链路裕量），增强方案鲁棒性 6. **多悬停点中继任务**：允许一架中继在一次任务内移动于两个邻近悬停点（需重新计算爬升能耗），可进一步压缩中继架次数——作为对比方案写入论文 7. **通信余量报告**：输出每个缺口区间的链路裕量（min(Lmax-Lpath)），证明方案对地形数据误差的鲁棒性

**Q4（补充分析）：** 8. **K=1..4 扫描**：除K=2/3外补充K=1（现状）与K=4，画出"分区数-总资源/缺口"曲线，揭示重复配置的规模效应，增强结论说服力 9. 工作量指标敏感性（用作业时间 vs 架次数 vs 货箱质量作为均衡度量对比）

## 五、实施与验证

1. 按A1→A5修复（预计1-2小时），B类顺手清理
2. 重跑 `problem1.py → problem2.py → problem3.py → problem4.py → export_results.py`，确认：结果数值变化仅限中继相关（A1影响悬停高海拔场景的架次能耗/时间）
3. 运行修复后的验证脚本：确认全部缺口区间全分辨率覆盖=0越限；Q3_通信保障.csv无空中继编号行
4. 若有GPU/时间余量，实施第四节建议1、2、4，生成Pareto图与下界对比表
5. 检查 `results/结果提交.xlsx` 六个sheet行数与列名与官方模板一致

**风险提示**：A1修复可能改变现有4个中继架次的能耗（若悬停点海拔未超巡航海拔则结果不变——需跑后确认）；A2/A3不影响当前结果（当前0未覆盖、候选点均在界内）。